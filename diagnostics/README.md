# Diagnostics

This directory is for local captures only. Diagnostic logs and macOS preference
snapshots are intentionally ignored because they can include host names,
operating-system build identifiers, file paths, and timing data.

Use `scripts/diagnose-mac.sh` on a Mac to create a fresh report locally. Share
only a redacted copy when reporting an issue.
